#Ternary: You have a variable is_member (Boolean). 
# If is_member is True, discount_rate is 0.10, else it's 0.02. 
# Use the ternary operator. Print the discount_rate.

is_member = True 
discount_rate = 0.1 if is_member else 0.02
print (discount_rate)

Rate = 10.00
variable = ('Rate' if is_member = True rate * 0.90 else rate * 0.08)

print (f"Member" {Member}, "Rate" {Rate})